# exam-2-montichello

🧾 Project Update — Monticello Website
🔹 Updated Features (latest changes)

Added navigation dots to the Hero slider for easier visual navigation.

Integrated the LightGallery (Lightbox) library — wrapped all images in the Gallery and Projects sections, now images open correctly in a modal window.

Implemented form validation (name & email fields) with clear console output and input checks.

Fixed JavaScript console errors (including undefined variables and event handling).

Cleaned up and optimized main.js structure — scroll animation, header visibility, and form submission work smoothly.

Verified that all external libraries (Slick, Atropos, LightGallery, Google Fonts) load correctly.

🔹 Technical Stack

HTML5 / SCSS / JS (ES6)

Slick Carousel

LightGallery (Lightbox)

Atropos.js

jQuery

Google Maps API

🔹 Validation

Form fields: required name and email

Console displays submitted data correctly

Input validation prevents empty submission

🔹 Fixes

Removed unused target variable (fixed runtime error in console).

Adjusted order of Google Maps script loading to prevent initMap callback errors.

Updated structure and spacing for Hero section (fixed layout stretching).

🔹 Next Steps

Improve responsive behavior for all screen sizes.

Add accessibility enhancements (aria-labels, focus states).

Refactor SCSS for further optimization.
