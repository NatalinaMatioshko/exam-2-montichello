# exam-2-montichello

🔧 Основна структура:

index.html — семантична розмітка сторінки з секціями:

header — логотип, навігаційне меню, соцмережі;

section\_\_2 (“Our Craft”) — інформаційний блок про кавові процеси з картинками;

section\_\_3 (“Coffee Journal”) — блок з картками статей у форматі слайдера;

section\_\_4 (“Gallery”) — грід-галерея кавових фотографій (велике зображення 2×2 + 4 малі);

footer — підвал (структура підготовлена під майбутній контент).

🎨 SCSS структура:

\_vars.scss — змінні кольорів, шрифтів, базових значень.

\_mixins.scss — міксини для повторюваних елементів (адаптив, flex, transition).

\_extends.scss — загальні стилі, що використовують @extend.

\_fonts.scss — підключення шрифтів (Montserrat, Open Sans).

\_common.scss — глобальні елементи: контейнер, секції, заголовки, текст.

main.scss — імпорти всіх частин, стилі для секцій (section**2, section**3, section\_\_4).

🧩 Реалізовані функції та правки:

повністю оновлено контент під кавову тематику;

адаптовано всі секції під максимальну ширину 1600px;

створено грід-галерею з 4 колонками, де перше фото займає дві колонки і два рядки;

кастомізовано кнопки у стилі кави (outline-варіант із плавним hover-ефектом);

оптимізовано структуру SCSS: видалені дублікати .btn_gall, упорядковані вкладення;

перевірено коректність @import і шляхи до всіх зображень (assets/img).

🖋️ Технології:

HTML5

SCSS (BEM, variables, mixins, extends)

Responsive design

### 🚀 Нові функції:

- Підключено **Slick Slider** для горизонтальної прокрутки карток.
- Оптимізовано футер: реалізовано **CSS-паралакс** через `background-attachment: fixed`.
- Покращено адаптивність і візуальну цілісність дизайну.

### 🧠 Використані бібліотеки:

- [Atropos.js](https://atroposjs.com/) — 3D tilt та глибина взаємодії.
- [Slick Carousel](http://kenwheeler.github.io/slick/) — адаптивний слайдер для карток Coffee Journal.
- jQuery 3.7.1 — базова взаємодія та ініціалізація плагінів.

### ✅ Поточна структура:

index.html
├── header (навігація, соцмережі)
├── section**2 (Our Craft)
├── section**3 (Coffee Journal + Atropos + Slick)
├── section\_\_4 (Gallery)
└── footer (з CSS-паралаксом)

## ✅ Актуальний стан

✅ Оновлення в проєкті

HTML:

Додано форму зворотного зв’язку в розділі Get in Touch.

У секції 2 (projects):

псевдоелементи тепер прикріплено до зовнішнього контейнера з правильними розмірами;

замінено зображення на більш релевантні;

прибрано відступ зліва у заголовка;

виправлено відступи між картинками.

У секції 3 (Coffee Journal):

реалізовано слайдер із кнопками керування з боків;

виправлено позицію та розміри карток.

У галереї (Gallery) кнопка «Load More» тепер вирівняна по центру.

Усі бекґраунди секцій зроблені на повну ширину екрана, без обрізання.

SCSS:

Перероблено структуру секцій, додано відступи та адаптивні брейкпоінти.

Виправлено позицію псевдоелементів і їхні розміри.

Оновлено стилі слайдера, кнопок і карток.

Уніфіковано кольори та відступи відповідно до змін у макеті.

JavaScript:

Додано ініціалізацію слайдера Coffee Journal (Slick).

Оновлено логіку головного слайдера Hero.

Додано обробку кліку для кнопок.

Оптимізовано порядок ініціалізації слайдерів (DRY).

👇

🔧 Оновлення

1. Секція Coffee Journal (Section 3)

Виправлено роботу слайдера: тепер він коректно відображає по 3 картки.

Вирівняно картки по висоті, додано рівномірні відступи між ними.

Dots перенесено під слайдер — розташовані по центру під усім блоком.

Додано стрілки навігації у вигляді круглих кнопок із кастомним SVG.

2. Форма зворотного зв’язку

Виправлено відступи та поведінку placeholder-ів.

Вирівняно елементи форми для адаптивних екранів.

Покращено UX при наведенні та фокусі.

3. Хедер

Додано поведінку при прокручуванні: хедер зникає після 3-ї секції.

Плавна анімація появи та приховування.

4. Загальні покращення

Оптимізовано стилі для узгодженого зовнішнього вигляду.

Виправлено дрібні візуальні та структурні помилки в SCSS і JS.

Код перевірено на відповідність принципу DRY.

🧠 Результат:
Сайт тепер має більш збалансований візуальний вигляд, узгоджені відступи,
анімації працюють плавно, і всі елементи виглядають як у дизайні.
